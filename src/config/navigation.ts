/**
 * レイアウト・ナビゲーション向けの再エクスポート。
 * 値の定義は `storefront.ts` が SSOT（重複定義を避ける）。
 */
export {
  FEATURED_COLLECTION_MENU_ITEMS,
  PRODUCTS_MENU_COLLECTION_ITEMS,
  SHOPIFY_CUSTOMER_ACCOUNT_URL,
  storefrontCollectionPath,
} from './storefront'

